# Use a lightweight base image with networking tools
FROM debian:latest

# Install hping3 and other dependencies
RUN apt-get update && apt-get install -y hping3 iproute2 && apt-get clean

# Set an entrypoint to make running commands easier
ENTRYPOINT ["hping3"]